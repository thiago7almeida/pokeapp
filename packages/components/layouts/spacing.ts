import {responsive} from '@components';

export const spacing = {
  s4: responsive.size(4),
  s8: responsive.size(8),
  s16: responsive.size(16),
  s24: responsive.size(24),
  s32: responsive.size(32),
};
