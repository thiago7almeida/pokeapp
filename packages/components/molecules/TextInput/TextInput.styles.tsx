import styled from 'styled-components/native';
import {spacing} from '../../layouts';

export const TextInputContainer = styled.View`
  border: 1px solid;
  border-color: gray;
  border-radius: ${spacing.s32}px;
  padding-horizontal: ${spacing.s16}px;
  margin: ${spacing.s8}px;
`;
